import { PageSkeleton } from "@/components/ui/PageSkeleton";

/** Galeri punya bentuk sendiri, jadi kerangkanya pun berbeda. */
export default function Loading() {
  return <PageSkeleton variant="gallery" />;
}
